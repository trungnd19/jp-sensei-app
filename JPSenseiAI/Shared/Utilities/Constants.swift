import Foundation

enum Constants {
    static let appGroupID = "group.com.jpsensei.shared"
    static let keychainGroup = "com.jpsensei.shared"

    enum StorageKeys {
        static let apiKey = "api_key"
        static let provider = "ai_provider"
        static let model = "ai_model"
        static let prompt = "system_prompt"
    }
}
